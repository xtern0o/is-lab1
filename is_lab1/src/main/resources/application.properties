database.url=jdbc:postgresql://${DB_HOST:pg}:${DB_PORT:5432}/${DB_NAME:studs}
database.username=${DB_USERNAME:s466127}
database.password=${DB_PASSWORD}
database.driver-class-name=org.postgresql.Driver
